{
  "name": "kcn-super-cognitive",
  "private": true,
  "version": "0.1.0",
  "description": "KCN Super Cognitive — monorepo root (frontend is the Vercel app)",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  },
  "engines": {
    "node": ">=20"
  }
}
