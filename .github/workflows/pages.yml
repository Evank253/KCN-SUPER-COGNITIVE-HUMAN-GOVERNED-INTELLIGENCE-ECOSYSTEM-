name: Deploy public demo to GitHub Pages

on: 
  push:
  branches: ["copilot/kcn-super-cognitive-ecosystems", "main", "develop"]
    workflow_dispatch:
   
    permissions:
  backend/app/routes/auth.py
 
  @@ -13,8 +13,8 @@
 
  from app.core.config import settings
  from app.core.security import (
  create_access_token,
  create_oauth_state_token,
  create_refresh_token,
  decode_token,
  is_authorized_github_admin,
  verify_oauth_state_token,





  backend/requirements-dev.txt
  @@ -1,4 +1,3 @@
  pytest==8.3.3
  pytest-asyncio==0.23.7
  httpx==0.27.0





  frontend/src/vite-env.d.TypeScript
  interface SpeechRecognitionAlternative {
  confidence:number
  }
  
  +declare const SpeechRecognition: {
    prototype: SpeechRecognition
    new (): SpeechRecognition
  }



...+23,9 @@ export default defineConfig {(
  },
 },
},
+ test:{
+   environment: 'jsdom',
+   globals: true,
+   setupfiles: './src/test/setup.ts',
+ },
 })
  deploy:
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
      - name: Upload public demo artifact
        uses: actions/upload-pages-artifact@v3